load("@hash_frx_pip//:requirements.bzl", "requirement")
load("@rules_python//python:defs.bzl", "py_test")

# Vectors read off the seven hand-written `_padding_tail` functions before they
# were deleted, plus one case per `PadRule` axis. No frx and no GPU_PLUGIN_DEPS:
# the padding is host arithmetic over a message LENGTH, so this needs no device.
py_test(
    name = "md_test",
    size = "small",
    srcs = ["md_test.py"],
    deps = [
        "//hash_frx/extension:md",
        requirement("absl_py"),
        requirement("numpy"),
    ],
)
